/* Crea una variable de tipo string. Reemplaza el valor de null por el correspondiente. */
var nuevoString = null;

// creando nueva variable de tipo string 
var cadena = 'palabra';

// reemplazando el valor null por el correspondiente
nuevoString = cadena;

// viendo en consola
console.log(nuevoString);

module.exports = nuevoString;